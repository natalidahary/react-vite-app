{
  "$schema": "./node_modules/nx/schemas/project-schema.json",
  "name": "productexplorer-workspace",
  "projectType": "library",
  "sourceRoot": ".",
  "tags": ["type:workspace"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"workspace build skipped\"",
        "cwd": "."
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"workspace lint skipped\"",
        "cwd": "."
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"workspace tests skipped\"",
        "cwd": "."
      }
    }
  }
}
